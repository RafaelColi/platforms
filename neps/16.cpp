#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    vector<int> cofre(n);

    for(int i = 0; i < n; i++) {
        cin >> cofre[i];
    }

    return 0;
}